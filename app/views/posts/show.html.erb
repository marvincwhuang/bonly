<%= image_tag @post.image.url(:medium) %>
<%= @post.title %>			
<%= @post.content %>
<%= link_to "Edit", edit_post_path %>
<%= link_to "Delete", post_path(@post), method: :delete, data: {comfirm: "Are you sure?"} %>

<%= link_to "Back", root_path  %>

